{
  "name": "bytes-radar-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Bytes Radar",
  "main": "worker.ts",
  "scripts": {
    "build": "node build.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "node build.js && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240208.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.28.1"
  }
}